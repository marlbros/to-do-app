{
	"name": "mern-notes-app",
	"version": "1.0.0",
	"description": "",
	"main": "app.js",
	"scripts": {
		"build": "cd front-end && npm install && npm run build",
		"client": " cd front-end && npm start",
		"server": "nodemon back-end/app.js",
		"start": "node back-end/app.js",
		"dev": "concurrently --kill-others-on-fail \"npm run server\" \"npm run client\"",
		"test": "echo \"Error: no test specified\" && exit 1"
	},
	"author": "Facundo Benitez",
	"license": "ISC",
	"dependencies": {
		"dotenv": "^16.0.0",
		"express": "^4.17.3",
		"mongoose": "^6.2.3"
	},
	"devDependencies": {
		"concurrently": "^7.0.0",
		"eslint": "^8.9.0",
		"nodemon": "^2.0.15"
	}
}